lab2、lab3 相关代码已分开打包，后端加载数据在 doucments 文件夹中。

项目具体启动流程如下：

    pip install -r requirements.txt
    run sever.py
    cd ./lab3/fronted
    npm install
    npm start

localhost:3000 为 lab3 查询页面，localhost:3000/lab2 为 lab2 查询页面。
